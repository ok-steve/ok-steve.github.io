<!DOCTYPE html>
<html lang="en-US">
  <head>
    {% include "head.njk" %}
  </head>
  <body class="[ center cover ] [ py--1 ]" typeof="schema:WebPage">
    <div class="cluster cluster-center">
      <header role="banner">
        <a href="/" rel="home" class="fz-6 ff-display">{{ site.name }}</a>
      </header>

      {% if site.env == 'development' %}
      {% include "navigation.njk" %}
      {% endif %}
    </div>

    <main class="cover-start stack" role="main">
      {{ content | safe }}
    </main>

    <footer class="cluster cluster-center" role="contentinfo">
      <p>{{ pkg.description }}</p>
      {% include "social-icons.njk" %}
    </footer>
  </body>
</html>
