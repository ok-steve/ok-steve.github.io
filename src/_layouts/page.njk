---
layout: default.njk
---
<section class="flow">
  {% if title %}
    <header>
      <h1>{{ title }}</h1>
    </header>
  {% endif %}

  <div class="flow text">
    {{ content | safe }}
  </div>
</section>
