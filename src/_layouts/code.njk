---
layout: post.njk
---

{{ content | safe }}

{% macro syntaxhighlight(code) %}
  {% if code.code %}
    {% highlight code.lang %}
      {{ code.code | safe }}
    {% endhighlight %}
  {% endif %}
{% endmacro %}

{{ syntaxhighlight(html) }}
{{ syntaxhighlight(css) }}
{{ syntaxhighlight(js) }}

<div class="h-0 overflow-hidden pt-9/16 relative border border-solid border-default">
  <iframe src="{{ 'embed.html' | url }}" class="absolute top-0 left-0 w-full h-full"></iframe>
</div>
