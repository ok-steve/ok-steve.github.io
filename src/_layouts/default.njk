<!DOCTYPE html>
<html lang="{{ page.lang | default(site.lang) | default('en') }}">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <title>{{ (renderData and renderData.title) or title or site.name }} | {{ site.name }}</title>
  <meta name="description" content="{{ page.excerpt | default(content) | default(site.description) | striptags | truncate(160) | escape }}">

  <link rel="apple-touch-icon" sizes="180x180" href="{{ "/assets/apple-touch-icon.png" | url }}">
  <link rel="apple-touch-startup-image" href="{{ "/assets/android-chrome-192x192.png" | url }}">
  <link rel="icon" type="image/png" href="{{ "/assets/favicon-32x32.png" | url }}" sizes="32x32">
  <link rel="icon" type="image/png" href="{{ "/assets/favicon-16x16.png" | url }}" sizes="16x16">
  <link rel="mask-icon" href="{{ "/assets/safari-pinned-tab.svg" | url }}" color="{{ site.color }}">
  <link rel="shortcut icon" href="{{ "/assets/favicon.ico" | url }}">

  <meta name="apple-mobile-web-app-title" content="{{ site.name | escape }}">
  <meta name="theme-color" content="{{ site.color }}">
  <meta name="application-name" content="{{ site.name | escape }}">
  <meta name="msapplication-TileColor" content="{{ site.color }}">
  <meta name="msapplication-config" content="{{ "/browserconfig.xml" | url }}">
  <link rel="manifest" href="{{ "/manifest.json" | url }}">

  <link rel="stylesheet" href="{{ "/css/main.css" | url }}">
  <link rel="canonical" href="{{ site.url }}{{ page.url | url }}">
  <link rel="alternate" type="application/rss+xml" title="{{ site.name | escape }}" href="{{ "/feed.xml" | url }}">
</head>

<body class="o-sticky-footer o-container" prefix="schema: http://schema.org/">
  <header role="banner">
    <h1><a href="{{ "/" | url }}">{{ site.name | escape }}</a></h1>
  </header>

  <nav role="navigation">
    {{ collections.all | eleventyNavigation | eleventyNavigationToHtml | safe }}
  </nav>

  <main class="o-sticky-footer__content" role="main">
    {{ content | safe }}
  </main>

  <footer role="contentinfo">
    <hr>

    <p>{{ site.description | escape }}</p>

    <ul class="u-list-inline">
      <li><a href="{{ "/feed.xml" | url }}">Feed</a></li>
      <li><a href="mailto:{{ site.email }}">Email</a></li>
      {%- if site.github_username -%}
      <li><a href="https://github.com/{{ site.github_username }}">Github</a></li>
      {%- endif -%}
      {%- if site.codepen_username -%}
      <li><a href="https://codepen.io/{{ site.codepen_username }}">Codepen</a></li>
      {%- endif -%}
      {%- if site.twitter_username -%}
      <li><a href="https://twitter.com/{{ site.twitter_username }}">Twitter</a></li>
      {%- endif -%}
      {%- if site.linkedin_username -%}
      <li><a href="https://linkedin.com/in/{{ site.linkedin_username }}">LinkedIn</a></li>
      {%- endif -%}
    </ul>
  </footer>

  <script src="{{ "/js/pjax.js" | url }}" defer></script>
</body>
</html>
