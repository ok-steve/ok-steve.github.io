<!DOCTYPE html>
<html lang="en-US">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>{{ title or site.name }} | {{ site.name }}</title>
    <meta
      name="description"
      content="{{ page.excerpt | default(content) | default(pkg.description) | md | striptags | truncate(160) }}"
    />

    <link rel="icon" href="/public/img/favicon.ico" size="any" />
    <link rel="icon" href="/public/img/icon.svg" type="image/svg+xml" />
    <link rel="apple-touch-icon" href="/public/img/apple-touch-icon.png" />
    <link rel="apple-touch-startup-image" href="/public/img/icon-512.png" />
    <meta name="apple-mobile-web-app-title" content="{{ site.name }}" />
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-status-bar-style" content="black" />
    <link rel="manifest" href="/manifest.webmanifest" />
    <meta name="theme-color" content="{{ site.color }}" />

    <link rel="canonical" href="{{ page.url | htmlBaseUrl(pkg.author.url) }}" />
    <link rel="alternate" type="application/atom+xml" title="{{ site.name }}" href="/feed.xml">

    <link
      rel="stylesheet"
      href="https://stevecherry.net/normcore/css/normcore.css"
    />
    <link rel="stylesheet" href="/public/syntax-highlighting.css" media="screen" />
    <link rel="stylesheet" href="/public/style.css" media="screen" />
    <link rel="stylesheet" href="/public/print.css" media="print" />

    <script src="/public/script.js" defer></script>

    <script defer>
      if ('serviceWorker' in navigator) {
        navigator.serviceWorker.register('{{ '/sw.js' | htmlBaseUrl }}', {
          scope: '{{ '/' | htmlBaseUrl }}',
        });
      }
    </script>
  </head>
  <body class="[ router ] [ cover stack ]" typeof="schema:WebPage">
    <div class="[ row ] [ pad-inline-0 ]">
      <header role="banner">
        <a href="/" class="scale-3">{{ site.name }}</a>
      </header>

      <nav role="navigation">
        <ul class="cluster" role="list">
          {% for entry in collections.all | eleventyNavigation %}
            <li>
              <a href="{{ entry.url }}"{% if entry.url == page.url %} aria-current="page"{% endif %}>{{ entry.title }}</a>
            </li>
          {% endfor %}
        </ul>
      </nav>
    </div>

    <main class="[ router__view ] [ container stack ] [ flex-auto fluid-type pad-inline-0 ]" role="main">
      {{ content | safe }}
    </main>

    <footer class="[ row ] [ pad-inline-0 pad-block-end-01 ]" role="contentinfo">
      <p>{{ pkg.description }}</p>

      <ul class="[ icon-list ] [ cluster ]" role="list">
        <li>
          <a href="/feed.xml">
            {% include "icons/rss.njk" %}
          </a>
        </li>
        <li>
          <a href="mailto:{{ pkg.author.email }}">
            {% include "icons/email.njk" %}
          </a>
        </li>
        <li>
          <a href="https://github.com/{{ site.github_username }}">
            {% include "icons/github.njk" %}
          </a>
        </li>
      </ul>
    </footer>
  </body>
</html>
