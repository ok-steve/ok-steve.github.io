{% extends "./default.njk" %}

{% block content %}
  <article class="flow">
    {% if title %}
      <header>
        <h1>{{ title }}</h1>
      </header>
    {% endif %}

    <div class="flow text">
      {{ content | safe }}
    </div>
  </article>
{% endblock %}
